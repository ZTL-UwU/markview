# Many code blocks

```rust
fn highlighted_0(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_1(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_2(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_3(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_4(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_5(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_6(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_7(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_8(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_9(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_10(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_11(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_12(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_13(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_14(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_15(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_16(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_17(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_18(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_19(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_20(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_21(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_22(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_23(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_24(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_25(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_26(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_27(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_28(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_29(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_30(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_31(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_32(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_33(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_34(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_35(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_36(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_37(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_38(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_39(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_40(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_41(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_42(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_43(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_44(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_45(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_46(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_47(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_48(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_49(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_50(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_51(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_52(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_53(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_54(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_55(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_56(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_57(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_58(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_59(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_60(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_61(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_62(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_63(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_64(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_65(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_66(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_67(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_68(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_69(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_70(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```rust
fn highlighted_71(value: usize) -> usize {
    let doubled = value * 2;
    if doubled > 10 { doubled } else { doubled + 1 }
}
```

```text
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

```
